[Unit]
Description=Ping Tunnel

[Service]
Type=simple
ExecStart=/usr/sbin/ptunnel -setuid root -setgid root -setcon -x {{ ansible_date_time.iso8601 | hash('md5') }}
Restart=on-abort

[Install]
WantedBy=multi-user.target
