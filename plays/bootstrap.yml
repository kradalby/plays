---
- name: Setup base configuration
  hosts: all

  roles:
    - apticron
    - { role: postfix, when: skip_postfix is not defined }
    - snmpd
    - { role: prometheus-node-exporter, when: skip_node_exporter is not defined }
